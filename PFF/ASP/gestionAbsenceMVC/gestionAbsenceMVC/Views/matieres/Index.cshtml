@model IEnumerable<gestionAbsenceMVC.Models.matiere>

@{
    ViewBag.Title = "Index";
}

<h2>Matiere</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.libelle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.libelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_M }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_M }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_M })
        </td>
    </tr>
}

</table>
