@model IEnumerable<gestionAbsenceMVC.Models.Prof>

@{
    ViewBag.Title = "Index";
}

<h2>Professor</h2>

<p>
    @Html.ActionLink("Create New", "Create")

</p>
<div class="form-horizontal">
    @using (Html.BeginForm("Index", "Profs", FormMethod.Get))
    {<b>search by :</b>
        @Html.RadioButton("searchBy", "Name", true)<text>  Name  </text>
        @Html.RadioButton("searchBy", "Gender")<text>  Gender  </text>
        @Html.RadioButton("searchBy", "Matiere")<text>  Matiere  </text>

        <p>
            @Html.TextBox("search")  <input type="submit" value="search" />
        </p>
    }

</div>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nom_P)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prenom_P)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sexe_P)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.matiere.libelle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nom_P)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prenom_P)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sexe_P)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.matiere.libelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_P }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_P }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_P })
        </td>
    </tr>
}

</table>
